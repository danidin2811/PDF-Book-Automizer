Core Task: Transcribe the Table of Contents from the attached PDF into a structured CSV file.

Technical Requirements:

    Tool Use: You must use the Python Data Analysis tool to create and save the file.

    Encoding: The file must be saved using the 'utf-8-sig' encoding (UTF-8 with BOM) to ensure Hebrew, phonetic symbols, and Greek characters are readable in Microsoft Excel.

    Download Link: Provide the standard system-generated download tag so the file can be saved to my device.

CSV Structure (Exactly 3 Columns):

    level: 1 for main sections/parts, 2 for chapters/articles, 3 for sub-points.

    title: The full heading text.

        Keep Right-to-Left (RTL) order.

        Preserve all Greek letters and phonetic symbols (e.g., d, h, s with diacritics).

        Remove the leader dots or bullets between the title and page number.

        Format as: "Article Title — Author Name" (if applicable).

    page_number: The associated digits.

Page Number Calculation:
The document has a page numbering offset of {offset}.
You must add this offset to every page number you find in the Table of Contents before writing it to the CSV.
Formula: (Page number printed in PDF) + ({offset}) = (Value for CSV page_number column).

Hierarchy: Determine the level based on indentation, font size, and the numbering system used in the document. Ensure all entries from the entire document are included.